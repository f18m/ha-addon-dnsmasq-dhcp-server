backend/bin/
backend/tmp/

frontend/node_modules/
frontend/.sass-cache/

# the CSS files are generated by the SASS compiler from the SCSS file-- no point in storing them in git
frontend/libs/dnsmasq-dhcp.css
frontend/libs/dnsmasq-dhcp.css.map

# all 3rd party CSS and JS libraries are downloaded by YARN -- no point in storing them in git
frontend/external-libs/

.docker-image
test-db.sqlite3
